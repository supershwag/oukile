


<div class="user-show">
  <div class="user-left-side-profile">
    <%= cl_image_tag current_user.photo, height: 200, width: 200, crop: :fill, class: "user-show-photo" %>
      <div class="buttons">

      </div>
  </div>
   <div class="user-details">
    <div class="tabs">
      <div class="tab active" data-section="about">
        <h3>About</h3>
      </div>
      <div class="tab" data-section="items">
        <h3>Items</h3>
      </div>
      <div class="tab" data-section="meetings">
        <h3>Meeting</h3>
      </div>
    </div>
    <div class="user-show-details" data-section="about">
      <p><%= current_user.first_name %> <%= current_user.last_name %></p>
      <p><%= current_user.email %></p>
      <%= link_to "Mettre à jour", edit_user_path(current_user), class: "user-button" %>
    </div>
    <div class="user-show-details hidden" data-section="items">
      <p>items <%= @user_items %></p>
    </div>
    <div class="user-show-details hidden" data-section="meetings">
      <p>meetings</p>
    </div>
  </div>
</div>
