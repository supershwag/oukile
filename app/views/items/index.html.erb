<div class="text-center">
  <h1>Tous nos objets trouvés</h1>
  <p>Vous avez perdu quelque chose ? Quelqu'un l'a peut-être trouvé !</p>
</div>

<div class="content">
  <div class="container">
    <div class="row">
      <% @items.each do |item| %>
        <% url = image_path('default.jpg') %>
        <div class="col-xs-12 col-sm-3">
          <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path item.photo, height: 300, width: 400, crop: :fill %>')">
            <div class="card-header">
              <h2 class="card-category">
                <%= link_to item.name, item_path(item) %>
              </h2>
              <div class="card-description text-left">
              <p> <%= item.category %></p>
               <p> <%= item.location %> - <%= Date.parse(item.date_found).strftime('%d-%m-%Y') %></p>
               </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <%= link_to "Ajouter un objet trouvé", new_item_path, class: "btn btn-primary text-center" %>
  </div>

  <div
    id="map"
    style="width: 100%;
    height: 600px;"
    data-markers="<%= @markers.to_json %>"
  ></div>
</div>

